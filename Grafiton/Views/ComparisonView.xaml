<Window x:Class="Grafiton.Views.ComparisonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
        xmlns:models="clr-namespace:Grafiton.Models"
        mc:Ignorable="d"
        Title="Comparaison de documents PDF" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <ui:TextBlock Text="Comparaison de documents (Superposition d'images)" FontTypography="Title" FontSize="22" FontWeight="Bold"/>
            <ui:TextBlock Text="Comparez 2 fichiers PDF et visualisez immédiatement les différences en surbrillance magenta." 
                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- File Pickers Grid -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Doc 1 -->
            <StackPanel Grid.Column="0">
                <ui:TextBlock Text="Document original (1) :" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding OriginalPdfPath}" Padding="8" IsReadOnly="True"/>
                    <ui:Button Grid.Column="1" Content="..." Margin="4,0,0,0" Command="{Binding BrowseOriginalCommand}"/>
                </Grid>
            </StackPanel>

            <!-- Doc 2 -->
            <StackPanel Grid.Column="2">
                <ui:TextBlock Text="Document modifié (2) :" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding ModifiedPdfPath}" Padding="8" IsReadOnly="True"/>
                    <ui:Button Grid.Column="1" Content="..." Margin="4,0,0,0" Command="{Binding BrowseModifiedCommand}"/>
                </Grid>
            </StackPanel>

            <!-- Start Button -->
            <ui:Button Grid.Column="4" Content="Comparer" Icon="ArrowCompare24" Appearance="Primary"
                       VerticalAlignment="Bottom" FontSize="15" Padding="20,8" Command="{Binding StartComparisonCommand}"/>
        </Grid>

        <!-- Main Display Area: Left Page Selector, Right Overlay Image -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Page list -->
            <Border Grid.Column="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="8">
                <ListBox ItemsSource="{Binding ComparisonResult.Pages}" SelectedItem="{Binding SelectedPageResult}">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:PageComparisonResult}">
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <ui:TextBlock Text="{Binding PageIndex, StringFormat='Page {0}'}" FontWeight="Bold"/>
                                <Border Margin="8,0,0,0" Padding="4,2" CornerRadius="4" Background="Magenta" Visibility="{Binding HasDifferences, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Différences" FontSize="10" Foreground="White" FontWeight="Bold"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Overlay Diff Image Viewport -->
            <Border Grid.Column="2" Background="Black" CornerRadius="8" Padding="12" ClipToBounds="True">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Image Source="{Binding SelectedPageResult.DiffImage}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Footer Status -->
        <StackPanel Grid.Row="3" Margin="0,16,0,0">
            <ui:TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource SystemAccentColorBrush}" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
