<UserControl x:Class="Grafiton.Views.BookmarkPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
             xmlns:models="clr-namespace:Grafiton.Models"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="260">

    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" 
            BorderThickness="0,0,1,0" 
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0" Text="Signets du document" FontTypography="Subtitle" Margin="0,0,0,12"/>

            <TreeView Grid.Row="1" ItemsSource="{Binding Bookmarks}" Background="Transparent" BorderThickness="0">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type models:BookmarkModel}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <ui:SymbolIcon Symbol="Bookmark24" FontSize="16" Margin="0,0,6,0" Foreground="{DynamicResource SystemAccentColorBrush}"/>
                            <ui:TextBlock Text="{Binding Title}" FontTypography="Body"/>
                            <ui:TextBlock Text="{Binding PageNumber, StringFormat=' (p.{0})'}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Grid>
    </Border>
</UserControl>
